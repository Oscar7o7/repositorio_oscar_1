
<div class="modal fade" id="modalCreateAddress" tabindex="-1" aria-labelledby="modalCreateAddressLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">

            <div class="modal-header">
                <h1 class="modal-title fs-5" id="modalCreateAddressLabel">{{form.title}}</h1>
                 
            </div>

            <div class="modal-body container-fluid">
                <form action="{{form.action}}" method="{{form.method}}" class="row">

                    <div class="form-floating col-12 mb-3">
                        <input class="form-control" name="description" id="address" value="{{data.description}}" type="text" placeholder="name@example.com" />
                        <label for="matricula">Dirección</label>
                    </div>

                    <div class="d-flex gap-3 justify-content-end">
                         
                        <button type="submit" class="btn btn-primary">
                            <i class="{{form.submit.ico}}"></i>
                            {{form.submit.text}}
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
