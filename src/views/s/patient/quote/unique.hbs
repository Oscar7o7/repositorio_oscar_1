<div class="card">
    <div class="card-body py-0 pt-2 d-flex justify-content-between">
        <h3>{{data.message}}</h3>
        <ul class="d-flex gap-4">
            <li style="list-style: none;">
                <a href="/patient/quote" class="btn btn-primary">
                    lista
                    <i class="bi bi-card-list"></i>
                </a>
            </li>
            {{#isDoctor user}}
            <li style="list-style: none;">
                <button class="btn btn-primary d-flex gap-2" data-toggle="modal" data-target="#modalAdminUser">
                    estados
                    <i class="bi bi-pen-fill"></i>
                </button>
            </li>
            {{/isDoctor}}
        </ul>
    </div>
</div>
<div class="card mby-3">
    <div class="card-body">
   
        <div class="card">
            <div class="card-body">
                {{#if data.doctorReference}}
                    <h5>Doctor(a) {{data.doctorReference.name}} {{data.doctorReference.lastname}}</h5>
                    
                    {{#if data.quoteDetailReference}}
                        <ul>
                            <li>Calificación: {{data.quoteDetailReference.starDoctor}}</li>
                            <li>Qualificación: {{data.quoteDetailReference.descriptionDoctor}}</li>
                            <li>Atendido: {{data.quoteDetailReference.endDateDoctor}}</li>
                        </ul>
                    {{/if}}
                {{else}}
                    <p class="lead text-muted">no hay doctor</p>
                {{/if}}
            </div>
        </div>

        <div class="card mt-3">
            <div class="card-body">
                {{#if data.patientReference}}
                    <h5>Paciente {{data.patientReference.name}} {{data.patientReference.lastname}}</h5>

                    {{#if data.quoteDetailReference}}
                        <ul>
                            <li>Calificación: {{data.quoteDetailReference.starPatient}}</li>
                            <li>Qualificación: {{data.quoteDetailReference.descriptionPatient}}</li>
                            <li>Atendido: {{data.quoteDetailReference.endDatePatient}}</li>
                        </ul>
                    {{/if}}
                {{else}}
                    <p class="lead text-muted">no hay paciente</p>
                {{/if}}
            </div>
        </div>

   </div>
</div>