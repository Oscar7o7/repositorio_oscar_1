<div class="row">
    <div class="col-12 m-auto">
        <div class="card mb-4">
            <div class="card-header pb-0 d-flex justify-content-between">
                <div>
                    <h6>{{currentPage.title}}</h6>
                </div>
                <div class="d-flex gap-3">
                    {{#each currentPage.actions}}
                        <a href="{{this.path}}" class="btn bg-gradient-primary">{{this.label}}</a>
                    {{/each}}
                </div>
            </div>
            <div class="card-body px-0 pt-0 pb-2">
                
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-12 col-lg-8 m-auto">
                            <form action="/insumo/category/create" method="post" class="">
                
                                <div class="container">
                                    <div class="row">
                                        <div class="col-12 col-lg-6">
                                            <label>Nombre</label>
                                            <input type="text" class="form-control" name="name" placeholder="Nombre" />
                                        </div>
                                    </div>
                                    <button type="submit" class="btn bg-gradient-primary mt-3">Crear</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>